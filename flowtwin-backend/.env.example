# Copy to .env and fill in. Required for AI narration.
LLM_PROVIDER=openai
LLM_API_KEY=sk-your-key-here
LLM_MODEL=gpt-4o-mini
# LLM_BASE_URL=https://api.openai.com/v1   # override for OpenAI-compatible providers

# PostgreSQL (defaults below match docker-compose.yml; only needed to override)
POSTGRES_DB=flowtwin
POSTGRES_USER=flowtwin
POSTGRES_PASSWORD=flowtwin
